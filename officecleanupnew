$dir = "C:\Temp\OfficeFix"
New-Item -ItemType Directory -Force -Path $dir

Start-BitsTransfer `
  -Source "https://aka.ms/OLicenseCleanup" `
  -Destination "$dir\OLicenseCleanup.zip"


Expand-Archive "$dir\OLicenseCleanup.zip" -DestinationPath $dir -Force



cscript //nologo C:\Temp\OfficeFix\OLicenseCleanup.vbs